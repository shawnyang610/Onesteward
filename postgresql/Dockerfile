# this Dockerfile is auto generated by running "create_postgres_dockerfile.py" script under ./web
# so the config is consistent with that of flask app's
FROM postgres:9.6

# Set environment variables
ENV POSTGRES_USER shawn
ENV POSTGRES_PASSWORD my_password
ENV POSTGRES_DB onesteward-db
